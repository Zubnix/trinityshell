package org.trinity.display.x11.core.api;

import org.trinity.foundation.display.api.DisplayProtocol;

public interface XDisplayProtocol extends DisplayProtocol {
	// ewmh & icccm not exposed to shell?
}
