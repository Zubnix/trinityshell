#This makefile is triggered by maven when build on an i386 linux compatible os arch.

#the target arch must match the value generated by 
#System.getProperty("os.arch") in Java.
LIBNAME_ARCH = i386
MARCH = i386

#After the LIBNAME_ARCH variable is set we launch the actual makefile
include Makefile.unix.shared